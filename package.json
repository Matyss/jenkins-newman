{
  "name": "jenkins-newman",
  "version": "1.0.0",
  "description": "Newman integration with Jenkins",
  "scripts": {
    "api-tests": "newman run NGWorkshop.collection.json -e NGWorkshop.environment.json -g globals.json --folder 'Workflows' --reporters junit,cli --reporter-junit-export report.xml"
  },
  "author": "Matyss",
  "license": "ISC",
  "dependencies": {
    "newman": "^4.4.0"
  }
}
